package external;

import java.util.function.Consumer;

public interface SLogoStringReturnable {
    /**
     * Returns the command that from the execution of the command object
     */
    public String returnValue();

}
